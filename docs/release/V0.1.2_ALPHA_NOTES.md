# RunBack v0.1.2-alpha

RunBack turns a completed public GitHub Actions failure URL into online evidence, a local replay
plan and a truthful reproduction verdict.

```bash
runback https://github.com/OWNER/REPO/actions/runs/RUN_ID
```

This release adds strict TypeScript compiler evidence after two preregistered JavaScript and
TypeScript compatibility runs.

## What changed

- Parse standard TypeScript diagnostics in the form
  `file.ts(line,column): error TS1234: message`.
- Require exact file, line, column, TypeScript diagnostic code, complete message and exit code
  before reporting `SAME_FAILURE`.
- Treat partially understood TypeScript error sets as insufficient evidence.
- Retain a custom JavaScript i18n failure as `INSUFFICIENT_EVIDENCE` instead of adding a
  repository-specific rule.
- Update the English and Chinese compatibility record with both first-run outcomes.

No Matcher threshold, Direct URL acquisition behavior, M1/M2 evidence, Docker daemon setting,
docker0 setting or host network setting changed.

## Real validation

Fresh Case F, `DeHubToken/dehub-mobile` run `34426920635`, reached its custom
`npm run i18n:coverage` failure. The visible remote and local diagnostics agreed, but no
supported structured identity existed, so the retained verdict is
`INSUFFICIENT_EVIDENCE`, `STEP`, `0/0/0`.

Fresh Case G, `ClickHouse/click-ui` run `34430830779`, initially returned
`INSUFFICIENT_EVIDENCE`, `STEP`, `0/0/0`. With the generic TypeScript parser, the unchanged URL
returned:

```text
Result: SAME_FAILURE
Evidence level: STRUCTURED
Remote failures: 3
Local failures:  3
Matched:         3
```

The frozen Werkzeug Case C also returned `SAME_FAILURE`, `TEST`, `1/1/1` using only its URL.
Flask and Click bundle checks, M1/M2 hashes, M2 step replay and full verify all passed.

## Install or upgrade

Linux amd64 users can install without Go or sudo:

```bash
curl -fsSL https://raw.githubusercontent.com/huanglinfei091-cmd/runback/main/scripts/install-release.sh | bash
~/.local/bin/runback doctor
```

Set `RUNBACK_INSTALL_VERSION=v0.1.2-alpha` when installing from a saved older installer.

## Current boundaries

The alpha targets completed failures in public GitHub.com repositories on `ubuntu-latest`.
Private repositories, GitHub Enterprise, Windows/macOS/self-hosted runners, services, job
containers, repository-local actions, reusable job workflows, dynamic matrices, secret-heavy
paths and artifact-heavy workflows remain outside the current scope. Node debug environments
are not yet supported by the narrow M2 debug flow.

External-user validation remains `USER_VALIDATION_PENDING`; project-owned real-case runs are
not counted as external users.
